--[[
    🍌 DELTA HUB - BANANA EDITION 🍌
    Author: Zo - Dành riêng cho Alpha
    Style: Giống Banana Hub 100%
]]

-- ==================================================
-- LOAD BANANA HUB UI
-- ==================================================
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/bloodball/-/master/BananaHubUILibrary"))()

local Window = Library:CreateWindow("🍌 DELTA HUB - BLOX FRUIT 🍌")

-- ==================================================
-- MAIN TAB
-- ==================================================
local MainTab = Window:NewTab("⚔️ Main")

-- Auto Farm Section
local FarmSection = MainTab:NewSection("Auto Farm")

FarmSection:CreateToggle("Auto Farm", nil, function(state)
    _G.AutoFarm = state
    while _G.AutoFarm do
        wait()
        -- Auto farm code
        local args = {
            [1] = "DealDamage",
            [2] = "Click"
        }
        game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(unpack(args))
    end
end)

FarmSection:CreateToggle("Auto Quest", nil, function(state)
    _G.AutoQuest = state
    while _G.AutoQuest do
        wait(1)
        local args = {
            [1] = "StartQuest",
            [2] = "BanditQuest1",
            [3] = 1
        }
        game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(unpack(args))
    end
end)

FarmSection:CreateToggle("Bring Mob", nil, function(state)
    _G.BringMob = state
    while _G.BringMob do
        wait(0.1)
        for _, mob in pairs(workspace.Enemies:GetChildren()) do
            if mob:FindFirstChild("Humanoid") and mob.Humanoid.Health > 0 then
                mob.HumanoidRootPart.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 10)
            end
        end
    end
end)

-- Devil Fruit Section
local FruitSection = MainTab:NewSection("Devil Fruit")

FruitSection:CreateToggle("Auto Store Fruit", nil, function(state)
    _G.AutoStore = state
    while _G.AutoStore do
        wait(0.5)
        for _, fruit in pairs(workspace:GetChildren()) do
            if fruit:IsA("Tool") and fruit:FindFirstChild("Handle") then
                local args = {
                    [1] = "StoreFruit",
                    [2] = fruit.Name
                }
                game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(unpack(args))
                fruit:Destroy()
            end
        end
    end
end)

FruitSection:CreateToggle("Fruit Sniper", nil, function(state)
    _G.FruitSniper = state
    while _G.FruitSniper do
        wait(1)
        for _, fruit in pairs(workspace:GetChildren()) do
            if fruit:IsA("Tool") and fruit:FindFirstChild("Handle") then
                local fruitName = fruit.Name:lower()
                if fruitName:find("dragon") or fruitName:find("leopard") or fruitName:find("venom") then
                    game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = fruit.Handle.CFrame
                    wait(0.5)
                    fireproximityprompt(fruit:FindFirstChildOfClass("ProximityPrompt"))
                end
            end
        end
    end
end)

-- ==================================================
-- RAID TAB
-- ==================================================
local RaidTab = Window:NewTab("⚡ Raid")

local RaidSection = RaidTab:NewSection("Raid Settings")

RaidSection:CreateToggle("Auto Raid", nil, function(state)
    _G.AutoRaid = state
    while _G.AutoRaid do
        wait(1)
        local args = {
            [1] = "Raids",
            [2] = "Select",
            [3] = "Flame"
        }
        game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(unpack(args))
        
        wait(1)
        
        local args2 = {
            [1] = "Raids",
            [2] = "Start"
        }
        game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(unpack(args2))
    end
end)

RaidSection:CreateToggle("Auto Awakening", nil, function(state)
    _G.AutoAwakening = state
    while _G.AutoAwakening do
        wait(1)
        for i = 1, 5 do
            local args = {
                [1] = "AwakeningChanger",
                [2] = "Buy",
                [3] = i
            }
            game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(unpack(args))
        end
    end
end)

-- ==================================================
-- MISC TAB
-- ==================================================
local MiscTab = Window:NewTab("🎮 Misc")

local MiscSection = MiscTab:NewSection("Other")

MiscSection:CreateToggle("ESP", nil, function(state)
    _G.ESP = state
    if _G.ESP then
        -- ESP code here
        for _, player in pairs(game:GetService("Players"):GetPlayers()) do
            if player ~= game.Players.LocalPlayer then
                local esp = Instance.new("Highlight")
                esp.Name = "ESP"
                esp.Adornee = player.Character
                esp.FillColor = Color3.fromRGB(255, 0, 0)
                esp.FillTransparency = 0.5
                esp.Parent = player.Character
            end
        end
    else
        for _, player in pairs(game:GetService("Players"):GetPlayers()) do
            if player.Character and player.Character:FindFirstChild("ESP") then
                player.Character.ESP:Destroy()
            end
        end
    end
end)

MiscSection:CreateToggle("Anti AFK", nil, function(state)
    _G.AntiAFK = state
    while _G.AntiAFK do
        wait(60)
        local VirtualUser = game:GetService("VirtualUser")
        VirtualUser:CaptureController()
        VirtualUser:ClickButton2(Vector2.new())
    end
end)

MiscSection:CreateSlider("Walk Speed", 16, 500, function(value)
    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = value
end)

MiscSection:CreateSlider("Jump Power", 50, 500, function(value)
    game.Players.LocalPlayer.Character.Humanoid.JumpPower = value
end)

-- ==================================================
-- TELEPORT TAB
-- ==================================================
local TeleportTab = Window:NewTab("📍 Teleport")

local Locations = {
    {"Jungle", CFrame.new(-1615.29663, 37.8177032, 136.452271)},
    {"Desert", CFrame.new(941.148682, 21.9741058, 1331.00757)},
    {"Snow", CFrame.new(1176.45642, 44.8183441, 1392.10388)},
    {"Marine", CFrame.new(-2604.70947, 6.35068512, 2014.25293)},
    {"Sky", CFrame.new(-4877.26611, 717.668213, -2622.5083)},
    {"Mansion", CFrame.new(-12459.7842, 334.97348, -7622.59521)},
    {"Castle", CFrame.new(-5222.11621, 22.8481827, -263.558563)},
    {"Pirate Village", CFrame.new(-1121.02307, 18.0950603, 3763.18896)},
}

for _, loc in ipairs(Locations) do
    TeleportTab:NewSection(loc[1]):CreateButton("Teleport", function()
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = loc[2]
    end)
end

-- ==================================================
-- STATS TAB
-- ==================================================
local StatsTab = Window:NewTab("📊 Stats")

local player = game.Players.LocalPlayer

StatsTab:NewSection("Player Info"):CreateLabel("Name: " .. player.Name)
StatsTab:NewSection("Player Info"):CreateLabel("Level: " .. player.Data.Level.Value)
StatsTab:NewSection("Player Info"):CreateLabel("Beli: " .. player.Data.Beli.Value)
StatsTab:NewSection("Player Info"):CreateLabel("Fragments: " .. player.Data.Fragments.Value)

StatsTab:NewSection("Stats"):CreateButton("Refill HP", function()
    local args = {
        [1] = "RefillHP"
    }
    game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(unpack(args))
end)

-- ==================================================
-- CREDITS
-- ==================================================
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "🍌 DELTA HUB",
    Text = "Loaded! Giống Banana Hub 100%",
    Duration = 3
})

print("✅ DELTA HUB LOADED - Banana Hub Style")